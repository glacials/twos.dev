<div class="photo-grid">
  <h2>{{ . }}</h2>
  {{ range (gallery .) }}
    <label class="gallery-item">
      <input type="checkbox" />
      <img alt="{{ .Alt }}" class="fullsize" src="{{ .WebPath }}" />
    </label>
  {{ end }}
</div>
