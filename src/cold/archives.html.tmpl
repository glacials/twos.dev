---
date: 2022-07-07
filename: archives.html
type: page
---

<h1>Writing</h1>

{{ $prev := (index (yearly posts) 0).Year }}
{{ range yearly posts }}
  {{ if lt .Year (sub $prev 1) }}
    <p>…</p>
  {{ end }}


  <h2>{{ .Year }}</h2>

  {{ range .Documents }}
    {{ with .Metadata }}
      <li>
        {{ with .Category }}{{ . }}:{{ end }}
        <a href="{{ .WebPath }}">{{ .Title }}</a>
      </li>
    {{ end }}
  {{ end }}
  {{ $prev = .Year }}
{{ end }}
