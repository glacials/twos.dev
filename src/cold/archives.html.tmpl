---
date: 2022-07-07
filename: archives.html
type: page
---

<h1>Writing</h1>

<p>
  <small>
    Feeds: <a href="/feed.atom">Atom (better)</a> /
    <a href="feed.rss">RSS</a>
  </small>
</p>

{{ with yearly posts }}
  {{ $prev := (index . 0).Year }}
  {{ range . }}
    {{ if lt .Year (sub $prev 1) }}
      <p>…</p>
    {{ end }}


    <h2>{{ .Year }}</h2>

    {{ range .Documents }}
      {{ with .Metadata }}
        <li>
          {{ with .Category }}{{ . }}:{{ end }}
          <a href="{{ .WebPath }}">{{ .Title }}</a>
        </li>
      {{ end }}
    {{ end }}
    {{ $prev = .Year }}
  {{ end }}
{{ end }}
