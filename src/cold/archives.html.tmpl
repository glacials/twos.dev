---
date: 2022-07-07
filename: archives.html
type: page
---

<h1>Writing</h1>

{{ $prev := (index archives 0).Year }}
{{ range archives }}
  {{ if lt .Year (sub $prev 1) }}
    <p>…</p>
  {{ end }}


  <h2>{{ .Year }}</h2>

  {{ range .Documents }}
    <li>
      {{ with .Category }}{{ . }}:{{ end }}
      <a href="{{ .Shortname }}.html">{{ .Title }}</a>
    </li>
  {{ end }}
  {{ $prev = .Year }}
{{ end }}
