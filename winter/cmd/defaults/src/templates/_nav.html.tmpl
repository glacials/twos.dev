<nav>
  <p style="text-align:center">
    <a
      href="/"
      {{ if eq .WebPath "index.html" }}class="selected"{{ end }}
      >Index</a
    >
  </p>

  <p style="text-align:center">
    {{ with parent }}
      <a href="{{ parent.WebPath }}">back to {{ parent.WebPath }}</a>
    {{ end }}
  </p>
</nav>
