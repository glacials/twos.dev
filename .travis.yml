language: node_js
node_js:
 - "node"

cache: yarn

script: yarn build

deploy:
  fqdn: twos.dev
 provider: pages
 skip_cleanup: true
 github_token: $GITHUB_TOKEN
 local_dir: dist
 on:
   branch: master
